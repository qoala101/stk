apiVersion: v1
kind: Service
metadata:
  name: service
  labels:
    app: stonks
spec:
  type: NodePort
  selector:
    app: stonks
  ports:
    - port: 80
      targetPort: sdb-port
      nodePort: 31111
